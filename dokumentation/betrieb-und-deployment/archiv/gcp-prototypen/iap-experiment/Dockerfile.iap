FROM node:22-alpine

WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY api/ ./api/
COPY .cloud-run-frontend/ ./public/

ENV NODE_ENV=production
ENV STATIC_ROOT=/app/public
EXPOSE 8080

CMD ["node", "api/server.mjs"]
