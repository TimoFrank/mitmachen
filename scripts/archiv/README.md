# Archivierte Skripte

Dieser Ordner enthaelt historische Hilfsskripte, die nicht zum aktiven Entwicklungs-, Build- oder Deployment-Pfad gehoeren.

## Dateien

- `build_postgres_import_from_supabase.mjs`: alter Importgenerator aus der GCP-/Cloud-SQL-Experimentierphase. Das Skript ist nur noch als Referenz abgelegt und nutzt den archivierten Schemaentwurf unter `dokumentation/betrieb-und-deployment/archiv/gcp-prototypen/postgres-migrationsentwurf/`.
