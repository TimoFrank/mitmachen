# System
.DS_Store

# Local Codex tooling (never part of the product repository)
/.codex-pet-runs/

# Dependencies
node_modules/

# Local environment
.env
.env.local

# Logs and test output
*.log
.weekly-release-notes.md
playwright-report/
test-results/
.playwright-cli/
/dist/

# Retired publication and export paths
/docs/
/output/
/outputs/

# Temporary Microsoft Office lock files
~$*

# Local data and backups
# The complete local hospitation workspace can contain private records and
# contact images. Keep it outside every commit and RC, not just its data file.
/frontend/local-hospitation/
/tests/local-hospitation.spec.js
*.sqlite
*.sqlite-shm
*.sqlite-wal
backups/
.gcp-decommission-evidence/
*_backup_*.csv
*_backup_*.json
backup-versorgungs-kompass-*.sql

# Local artifact build, render and audit workspaces
/.artifact-build-*/
/.artifact-word-update.*/
/.artifact-revision.*/
/.docx-memo-build.*/
/.tmp-ppt-audit.*/
/.tmp-template-audit-*/
/tmp/
